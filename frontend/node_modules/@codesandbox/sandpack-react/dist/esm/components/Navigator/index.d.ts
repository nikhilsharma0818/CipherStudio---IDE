import * as React from "react";
/**
 * @category Components
 */
export interface NavigatorProps {
    clientId: string;
    onURLChange?: (newURL: string) => void;
}
/**
 * @category Components
 */
export declare const Navigator: ({ clientId, onURLChange, className, ...props }: NavigatorProps & React.HTMLAttributes<HTMLDivElement>) => JSX.Element;
