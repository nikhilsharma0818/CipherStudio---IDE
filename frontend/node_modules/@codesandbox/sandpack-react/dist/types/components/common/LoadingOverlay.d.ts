import * as React from "react";
/**
 * @category Components
 */
export interface LoadingOverlayProps {
    clientId?: string;
    /**
     * It enforces keeping the loading state visible,
     * which is helpful for external loading states.
     */
    loading?: boolean;
    showOpenInCodeSandbox: boolean;
}
/**
 * @category Components
 */
export declare const LoadingOverlay: ({ clientId, loading, className, style, showOpenInCodeSandbox, ...props }: LoadingOverlayProps & React.HTMLAttributes<HTMLDivElement>) => JSX.Element | null;
